Good. Now I have a comprehensive picture. Let me also check the remaining domain API schemas for user_id requirement:Let me verify one key thing — whether the food/search endpoint requires `user_id` as a query param, which would break the frontend call:

