[project]
name = "t1d-companion"
version = "2.0.0"
description = "Glucose forecasting companion (educational)"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"

dependencies = [
    "pydantic>=2.0",
    "httpx>=0.25",
]
